apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: devops-toolkit

spec:
  replicas: 1

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
      - name: backend
        image: paul48/devops-backend:latest

        ports:
        - containerPort: 5000
